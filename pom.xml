<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>se.moshicon</groupId>
  <artifactId>maven-server-credentials-exporter</artifactId>
  <version>1.0.0</version>
  <packaging>maven-plugin</packaging>

  <name>maven-server-credentials-exporter</name>
  <description>Expose credentials from Maven settings.xml servers as Maven system properties.</description>
  <url>https://github.com/moshicon/maven-server-credentials-exporter</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <maven.api.version>3.9.6</maven.api.version>
    <maven.plugin.annotations.version>3.10.1</maven.plugin.annotations.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-plugin-api</artifactId>
      <version>${maven.api.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-core</artifactId>
      <version>${maven.api.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.maven.plugin-tools</groupId>
      <artifactId>maven-plugin-annotations</artifactId>
      <version>${maven.plugin.annotations.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-plugin-plugin</artifactId>
      <version>3.15.2</version>
      <configuration>
        <goalPrefix>server-credentials</goalPrefix>
      </configuration>
      <executions>
        <execution>
          <goals>
            <goal>descriptor</goal>
          </goals>
        </execution>
      </executions>
    </plugin>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-invoker-plugin</artifactId>
      <version>3.9.1</version>
      <configuration>
        <projectsDirectory>${project.basedir}/src/it</projectsDirectory>
        <cloneProjectsTo>${project.build.directory}/its</cloneProjectsTo>
        <cloneAllFiles>true</cloneAllFiles>
        <failIfNoProjects>true</failIfNoProjects>
        <localRepositoryPath>${project.build.directory}/it-repo</localRepositoryPath>
        <mergeUserSettings>true</mergeUserSettings>
        <pomIncludes>
          <pomInclude>*/pom.xml</pomInclude>
        </pomIncludes>
        <filterProperties>
          <project.version>${project.version}</project.version>
        </filterProperties>
      </configuration>
      <executions>
        <execution>
          <id>run-integration-tests</id>
          <phase>verify</phase>
          <goals>
            <goal>integration-test</goal>
            <goal>verify</goal>
          </goals>
        </execution>
      </executions>
    </plugin>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-install-plugin</artifactId>
      <version>3.1.4</version>
      <executions>
        <execution>
          <id>stage-plugin-for-it</id>
          <phase>pre-integration-test</phase>
          <goals>
            <goal>install-file</goal>
          </goals>
          <configuration>
            <file>${project.build.directory}/${project.build.finalName}.jar</file>
            <groupId>${project.groupId}</groupId>
            <artifactId>${project.artifactId}</artifactId>
            <version>${project.version}</version>
            <packaging>${project.packaging}</packaging>
            <localRepositoryPath>${project.build.directory}/it-repo</localRepositoryPath>
          </configuration>
        </execution>
      </executions>
    </plugin>
    </plugins>
  </build>
</project>
